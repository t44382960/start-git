# 关联本地仓库和远程仓库

```shell
# 直接克隆远程仓库到本地
git clone xxx
# 新建本地仓库与远程仓库关联
mkdir local-repo
cd local-repo
git init
git remote add orgin xxx
# 查看关联的远程仓库
git remote -v
# 将本地仓库的main分支与远程仓库的main分支关联起来
git push -u orgin main:main #如果分支名字相同写一个即可

# 推送
git push
# 拉取
git pull
```

